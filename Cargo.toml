[package]
# Keep in sync with `PACKAGE` in `Makefile`
name = "licensekey_handler"
version = "1.0.0"
edition = "2021"

[dependencies]
bindgen = "0.69.4"
env_logger = "0.11.2"
glib-sys = "0.19.5"
libc = "0.2.153"
log = "0.4.20"
pkg-config = "0.3.30"
syslog = "6.1.1"
thiserror = "1.0.57"

licensekey = { git = "https://github.com/AxisCommunications/acap-rs.git" }