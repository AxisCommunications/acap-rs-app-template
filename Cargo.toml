[package]
# Keep in sync with both
# - `acapPackageConf.setup.appName` in `manifest.json`
# - `PACKAGE` in `Makefile`
name = "hello_world"
version = "1.0.0"
edition = "2021"

[dependencies]
acap-logging = "0.0.0"
log = "0.4.21"

